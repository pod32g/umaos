name: Laptop
version: 1
icon: uma-laptop
description: "Battery optimization and power management for portable use."
packages:
  pacman:
    - power-profiles-daemon
  flatpak: []
  aur:
    # AUR-only package (not in official repos); under pacman: it failed the
    # whole batch and the auto-cpufreq.service enable that follows.
    - auto-cpufreq
configs: []
services:
  enable:
    - auto-cpufreq.service
post_install: []
